# Contributing

- Use pull requests (no direct commits to `main`).
- Keep changes minimal and aligned with the documented constraints in [docs/architecture.md](docs/architecture.md).
- Ensure CI passes (`make lint` and `./tests/bin/run-bats.sh`).
