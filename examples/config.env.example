# Example configuration for /etc/kiosk-retropie/config.env

# Repo pinning for the one-time installer bootstrap (required)
KIOSK_RETROPIE_REPO_URL=https://github.com/theaussiepom/kiosk-retropie.git
KIOSK_RETROPIE_REPO_REF=main

KIOSK_URL=
NFS_SERVER=
NFS_PATH=

# ROM sync (Pattern A)
# ROMs are copied from NFS on boot to a local directory.
# Gameplay does not depend on NFS being available.
#
# Optional:
# KIOSK_RETROPIE_NFS_MOUNT_POINT=/mnt/kiosk-retropie-roms
# KIOSK_RETROPIE_NFS_MOUNT_OPTIONS=ro
# KIOSK_RETROPIE_NFS_ROMS_SUBDIR=roms
# KIOSK_RETROPIE_ROMS_DIR=/var/lib/kiosk-retropie/retropie/roms
# KIOSK_RETROPIE_ROMS_SYNC_DELETE=0
# KIOSK_RETROPIE_ROMS_OWNER=retropi:retropi
# KIOSK_RETROPIE_ROMS_SYSTEMS=nes,snes,megadrive,psx
# KIOSK_RETROPIE_ROMS_EXCLUDE_SYSTEMS=ps2,ps3,switch

# Optional display rotation
# KIOSK_RETROPIE_SCREEN_ROTATION=normal

# Controller button codes (optional)
#
# This project uses Linux evdev button codes. Different controllers can emit
# different codes, so you can configure them here.
#
# To discover codes on the Pi:
#   sudo kiosk-retropie-controller-codes.sh
#   # then press the buttons you want.
#
# Enter Retro (HA -> Retro):
# KIOSK_RETROPIE_RETRO_ENTER_TRIGGER_CODE=315
#
# Exit Retro (Retro -> HA): press KIOSK_RETROPIE_RETRO_EXIT_SECOND_CODE, then press
# KIOSK_RETROPIE_RETRO_EXIT_TRIGGER_CODE within KIOSK_RETROPIE_COMBO_WINDOW_SEC.
# KIOSK_RETROPIE_RETRO_EXIT_TRIGGER_CODE=315
# KIOSK_RETROPIE_RETRO_EXIT_SECOND_CODE=304
#
# Timing knobs (optional):
# KIOSK_RETROPIE_START_DEBOUNCE_SEC=1.0
# KIOSK_RETROPIE_COMBO_WINDOW_SEC=0.75

# Local save/state directories (always local)
# KIOSK_RETROPIE_SAVES_DIR=/var/lib/kiosk-retropie/retropie/saves
# KIOSK_RETROPIE_STATES_DIR=/var/lib/kiosk-retropie/retropie/states

# Optional: periodic backup of saves/states to NFS (skips while in Retro mode)
# KIOSK_RETROPIE_SAVE_BACKUP_ENABLED=0
# KIOSK_RETROPIE_SAVE_BACKUP_DIR=/mnt/kiosk-retropie-backup
# KIOSK_RETROPIE_SAVE_BACKUP_SUBDIR=kiosk-retropie-saves
# KIOSK_RETROPIE_SAVE_BACKUP_DELETE=0
# KIOSK_RETROPIE_SAVE_BACKUP_NFS_SERVER=
# KIOSK_RETROPIE_SAVE_BACKUP_NFS_PATH=
# KIOSK_RETROPIE_SAVE_BACKUP_NFS_MOUNT_OPTIONS=rw

# Optional: install RetroPie during first boot (heavy; may take a long time)
# KIOSK_RETROPIE_INSTALL_RETROPIE=0

# LED MQTT control (optional)
KIOSK_RETROPIE_LED_MQTT_ENABLED=0
MQTT_HOST=
MQTT_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_TLS=0
KIOSK_RETROPIE_MQTT_TOPIC_PREFIX=kiosk-retropie
