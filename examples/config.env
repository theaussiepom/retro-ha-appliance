# Example configuration for retro-ha-appliance
# Copy to: /etc/retro-ha/config.env

# Repo pinning for the one-time installer bootstrap (required)
RETRO_HA_REPO_URL=https://github.com/theaussiepom/retro-ha-appliance.git
RETRO_HA_REPO_REF=main

# Enable the MQTT LED control agent so Home Assistant can toggle LEDs.
RETRO_HA_LED_MQTT_ENABLED=1

# MQTT connection (set these to match your broker)
MQTT_HOST=192.168.1.10
MQTT_PORT=1883

# Credentials are optional.
# - If your Mosquitto requires auth, set MQTT_USERNAME + MQTT_PASSWORD.
# - If your broker is anonymous, leave them unset.
MQTT_USERNAME=your_username
MQTT_PASSWORD=your_password

# Optional TLS toggle for mosquitto-clients.
# MQTT_TLS=0

# Topic prefix used by scripts/leds/led-mqtt.sh
RETRO_HA_MQTT_TOPIC_PREFIX=retro-ha

# LED sysfs names (Pi defaults are usually led0=ACT, led1=PWR)
RETRO_HA_ACT_LED=led0
RETRO_HA_PWR_LED=led1

# Triggers to restore when turning LEDs on
RETRO_HA_ACT_LED_TRIGGER_ON=mmc0
RETRO_HA_PWR_LED_TRIGGER_ON=default-on
